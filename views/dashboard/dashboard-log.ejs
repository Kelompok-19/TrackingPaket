<%- include('../layout/header', {title : "Jan-Express - Dashboard Edit"}) %>
<br>
<br>
<form id="log" name="log" method="post" action="<%= locals.current_path %>">
    <div class="data container">
        <h2>
            <a class="" href="/dashboard">
                <i data-feather="arrow-left-circle" style="margin-right: 0.5cm; color: grey;"></i>
            </a>
            Detail log
        </h2>
        <hr>
        <div class="row">
            <div class="col-sm">
                <label for="tipe_status">Status : </label><br>
                <select name="tipe_status">
                    <% for (i in logtypes) { %>
                        <option value="<%= logtypes[i].type_id %>"><%= logtypes[i].type_name %></option>
                    <% } %>
                </select>
            </div>
        </div>
        <div class="row">
            <div class="col-sm">
                <label for="message">Pesan : </label><br>
                <input class="text" name="message" type="text" required /><br>
            </div>
        </div>        
        <br>
        <input class="button" type="submit" value="Submit" />
        </div>
    <% if (locals.message) { %>
        <h2><%= message %></h2>
    <% } %>
</form>
<%- include('../layout/footer') %>